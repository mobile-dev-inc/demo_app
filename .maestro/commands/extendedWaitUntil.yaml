appId: com.example.example
tags:
  - passing
---
- launchApp # For idempotence of sections

- extendedWaitUntil:
    timeout: 10000
    visible:
      text: 'Gesture Tester'

- extendedWaitUntil:
    timeout: 100
    notVisible:
      text: 'Non Existent Text'
